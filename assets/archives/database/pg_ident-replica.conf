# PostgreSQL User Name Mapping Configuration - Replica
# E-commerce Application Identity Mapping
# This file controls user name mapping for authentication on the replica server

# MAPNAME       SYSTEM-USERNAME         PG-USERNAME

# Default mapping - system postgres user maps to postgres database user
postgres        postgres                postgres

# Replication user mapping
replication     postgres                replicator

# Application user mappings (for future use)
# app             ecommerce              app_user
# readonly        readonly               readonly_user

# OS user to database user mappings
# Map local system users to appropriate database roles
# Format: mapname system-username pg-username

# Example SSL certificate mappings (for future SSL client authentication)
# ssl-cert      /CN=replica.local       replicator
# ssl-cert      /CN=app.local          app_user

# End of pg_ident.conf